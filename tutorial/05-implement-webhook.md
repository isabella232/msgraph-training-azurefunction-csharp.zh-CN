---
ms.openlocfilehash: f55eecd4d157c945d0e95870d4e481653b26c9ec
ms.sourcegitcommit: 57a5c2e1a562d8af092a3e78786d711ce1e8f9cb
ms.translationtype: MT
ms.contentlocale: zh-CN
ms.lasthandoff: 10/30/2020
ms.locfileid: "48822560"
---
<!-- markdownlint-disable MD002 MD041 -->

<span data-ttu-id="3fddd-101">在本练习中，你将完成实现 Azure 功能 `SetSubscription` 和 `Notify` 更新测试应用程序以订阅和取消订阅用户收件箱中的更改。</span><span class="sxs-lookup"><span data-stu-id="3fddd-101">In this exercise you will finish implementing the Azure Functions `SetSubscription` and `Notify`, and update the test application to subscribe and unsubscribe to changes in a user's inbox.</span></span>

- <span data-ttu-id="3fddd-102">`SetSubscription`函数将充当 API，允许测试应用创建或删除对用户的收件箱中的更改的[订阅](https://docs.microsoft.com/graph/webhooks)。</span><span class="sxs-lookup"><span data-stu-id="3fddd-102">The `SetSubscription` function will act as an API, allowing the test app to create or delete a [subscription](https://docs.microsoft.com/graph/webhooks) to changes in a user's inbox.</span></span>
- <span data-ttu-id="3fddd-103">该 `Notify` 函数将充当接收由订阅生成的更改通知的 webhook。</span><span class="sxs-lookup"><span data-stu-id="3fddd-103">The `Notify` function will act as the webhook that receives change notifications generated by the subscription.</span></span>

<span data-ttu-id="3fddd-104">这两个函数都将使用 [客户端凭据授予流](https://docs.microsoft.com/azure/active-directory/develop/v2-oauth2-client-creds-grant-flow) 获取用于调用 Microsoft Graph 的仅应用程序令牌。</span><span class="sxs-lookup"><span data-stu-id="3fddd-104">Both functions will use the [client credentials grant flow](https://docs.microsoft.com/azure/active-directory/develop/v2-oauth2-client-creds-grant-flow) to get an app-only token to call Microsoft Graph.</span></span> <span data-ttu-id="3fddd-105">由于管理员向管理员授予所需的权限范围，因此无需任何用户交互即可获取令牌。</span><span class="sxs-lookup"><span data-stu-id="3fddd-105">Because an administrator granted admin consent to the required permission scopes, no user interaction will be required to get the token.</span></span>

## <a name="add-client-credentials-authentication-to-the-azure-functions-project"></a><span data-ttu-id="3fddd-106">将客户端凭据身份验证添加到 Azure 函数项目</span><span class="sxs-lookup"><span data-stu-id="3fddd-106">Add client credentials authentication to the Azure Functions project</span></span>

<span data-ttu-id="3fddd-107">在本节中，你将在 Azure 函数项目中实现客户端凭据流，以获取与 Microsoft Graph 兼容的访问令牌。</span><span class="sxs-lookup"><span data-stu-id="3fddd-107">In this section you'll implement the client credentials flow in the Azure Functions project to get an access token compatible with Microsoft Graph.</span></span>

1. <span data-ttu-id="3fddd-108">在包含 **GraphTutorial** 的目录中打开您的 CLI。</span><span class="sxs-lookup"><span data-stu-id="3fddd-108">Open your CLI in the directory that contains **GraphTutorial.csproj**.</span></span>

1. <span data-ttu-id="3fddd-109">使用以下命令将 webhook 应用程序 ID 和密码添加到密钥存储中。</span><span class="sxs-lookup"><span data-stu-id="3fddd-109">Add your webhook application ID and secret to the secret store using the following commands.</span></span> <span data-ttu-id="3fddd-110">将替换为 `YOUR_WEBHOOK_APP_ID_HERE` **Graph Azure 函数 Webhook** 的应用程序 ID。</span><span class="sxs-lookup"><span data-stu-id="3fddd-110">Replace `YOUR_WEBHOOK_APP_ID_HERE` with the application ID for the **Graph Azure Function Webhook**.</span></span> <span data-ttu-id="3fddd-111">将替换为 `YOUR_WEBHOOK_APP_SECRET_HERE` 在 azure 门户中为 **Graph Azure 函数 Webhook** 创建的应用程序密码。</span><span class="sxs-lookup"><span data-stu-id="3fddd-111">Replace `YOUR_WEBHOOK_APP_SECRET_HERE` with the application secret you created in the Azure portal for the **Graph Azure Function Webhook**.</span></span>

    ```Shell
    dotnet user-secrets set webHookId "YOUR_WEBHOOK_APP_ID_HERE"
    dotnet user-secrets set webHookSecret "YOUR_WEBHOOK_APP_SECRET_HERE"
    ```

### <a name="create-a-client-credentials-authentication-provider"></a><span data-ttu-id="3fddd-112">创建客户端凭据身份验证提供程序</span><span class="sxs-lookup"><span data-stu-id="3fddd-112">Create a client credentials authentication provider</span></span>

1. <span data-ttu-id="3fddd-113">在名为 **ClientCredentialsAuthProvider.cs** 的 **/GraphTutorial/Authentication** 目录中创建一个新文件，并添加以下代码。</span><span class="sxs-lookup"><span data-stu-id="3fddd-113">Create a new file in the **./GraphTutorial/Authentication** directory named **ClientCredentialsAuthProvider.cs** and add the following code.</span></span>

    :::code language="csharp" source="../demo/GraphTutorial/Authentication/ClientCredentialsAuthProvider.cs" id="AuthProviderSnippet":::

<span data-ttu-id="3fddd-114">请花点时间考虑 **ClientCredentialsAuthProvider.cs** 中的代码的功能。</span><span class="sxs-lookup"><span data-stu-id="3fddd-114">Take a moment to consider what the code in **ClientCredentialsAuthProvider.cs** does.</span></span>

- <span data-ttu-id="3fddd-115">在构造函数中，它从包中初始化 **ConfidentialClientApplication** `Microsoft.Identity.Client` 。</span><span class="sxs-lookup"><span data-stu-id="3fddd-115">In the constructor, it initializes a **ConfidentialClientApplication** from the `Microsoft.Identity.Client` package.</span></span> <span data-ttu-id="3fddd-116">它使用 `WithAuthority(AadAuthorityAudience.AzureAdMyOrg, true)` 和 `.WithTenantId(tenantId)` 函数将登录访问群体限制为仅指定的 Microsoft 365 组织。</span><span class="sxs-lookup"><span data-stu-id="3fddd-116">It uses the `WithAuthority(AadAuthorityAudience.AzureAdMyOrg, true)` and `.WithTenantId(tenantId)` functions to restrict the login audience to only the specified Microsoft 365 organization.</span></span>
- <span data-ttu-id="3fddd-117">在 `GetAccessToken` 函数中，它调用 `AcquireTokenForClient` 以获取应用程序的令牌。</span><span class="sxs-lookup"><span data-stu-id="3fddd-117">In the `GetAccessToken` function, it calls `AcquireTokenForClient` to get a token for the application.</span></span> <span data-ttu-id="3fddd-118">客户端凭据令牌流始终是非交互式的。</span><span class="sxs-lookup"><span data-stu-id="3fddd-118">The client credentials token flow is always non-interactive.</span></span>
- <span data-ttu-id="3fddd-119">它实现 `Microsoft.Graph.IAuthenticationProvider` 接口，允许在中的构造函数中传递此类以对 `GraphServiceClient` 传出请求进行身份验证。</span><span class="sxs-lookup"><span data-stu-id="3fddd-119">It implements the `Microsoft.Graph.IAuthenticationProvider` interface, allowing this class to be passed in the constructor of the `GraphServiceClient` to authenticate outgoing requests.</span></span>

## <a name="update-graphclientservice"></a><span data-ttu-id="3fddd-120">更新 GraphClientService</span><span class="sxs-lookup"><span data-stu-id="3fddd-120">Update GraphClientService</span></span>

1. <span data-ttu-id="3fddd-121">打开 **GraphClientService.cs** ，并将以下属性添加到类中。</span><span class="sxs-lookup"><span data-stu-id="3fddd-121">Open **GraphClientService.cs** and add the following property to the class.</span></span>

    :::code language="csharp" source="../demo/GraphTutorial/Services/GraphClientService.cs" id="AppGraphClientMembers":::

1. <span data-ttu-id="3fddd-122">将现有的 `GetAppGraphClient` 函数替换为以下内容。</span><span class="sxs-lookup"><span data-stu-id="3fddd-122">Replace the existing `GetAppGraphClient` function with the following.</span></span>

    :::code language="csharp" source="../demo/GraphTutorial/Services/GraphClientService.cs" id="AppGraphClientMembers":::

## <a name="implement-notify-function"></a><span data-ttu-id="3fddd-123">实现通知函数</span><span class="sxs-lookup"><span data-stu-id="3fddd-123">Implement Notify function</span></span>

<span data-ttu-id="3fddd-124">在本节中，您将实现 `Notify` 将用作更改通知的通知 URL 的函数。</span><span class="sxs-lookup"><span data-stu-id="3fddd-124">In this section you'll implement the `Notify` function, which will be used as the notification URL for change notifications.</span></span>

1. <span data-ttu-id="3fddd-125">在名为 " **模型** " 的 **GraphTutorials** 目录中创建一个新目录。</span><span class="sxs-lookup"><span data-stu-id="3fddd-125">Create a new directory in the **GraphTutorials** directory named **Models**.</span></span>

1. <span data-ttu-id="3fddd-126">在名为 " **ResourceData.cs** " 的 **模型** 目录中创建一个新文件，并添加以下代码。</span><span class="sxs-lookup"><span data-stu-id="3fddd-126">Create a new file in the **Models** directory named **ResourceData.cs** and add the following code.</span></span>

    :::code language="csharp" source="../demo/GraphTutorial/Models/ResourceData.cs" id="ResourceDataSnippet":::

1. <span data-ttu-id="3fddd-127">在名为 " **ChangeNotification.cs** " 的 **模型** 目录中创建一个新文件，并添加以下代码。</span><span class="sxs-lookup"><span data-stu-id="3fddd-127">Create a new file in the **Models** directory named **ChangeNotification.cs** and add the following code.</span></span>

    :::code language="csharp" source="../demo/GraphTutorial/Models/ChangeNotification.cs" id="ChangeNotificationSnippet":::

1. <span data-ttu-id="3fddd-128">在名为 " **NotificationList.cs** " 的 **模型** 目录中创建一个新文件，并添加以下代码。</span><span class="sxs-lookup"><span data-stu-id="3fddd-128">Create a new file in the **Models** directory named **NotificationList.cs** and add the following code.</span></span>

    :::code language="csharp" source="../demo/GraphTutorial/Models/NotificationList.cs" id="NotificationListSnippet":::

1. <span data-ttu-id="3fddd-129">打开 **/GraphTutorial/Notify.cs** ，并将其全部内容替换为以下内容。</span><span class="sxs-lookup"><span data-stu-id="3fddd-129">Open **./GraphTutorial/Notify.cs** and replace its entire contents with the following.</span></span>

    :::code language="csharp" source="../demo/GraphTutorial/Notify.cs" id="NotifySnippet":::

<span data-ttu-id="3fddd-130">请花点时间考虑 **Notify.cs** 中的代码的功能。</span><span class="sxs-lookup"><span data-stu-id="3fddd-130">Take a moment to consider what the code in **Notify.cs** does.</span></span>

- <span data-ttu-id="3fddd-131">`Run`函数检查是否存在 `validationToken` 查询参数。</span><span class="sxs-lookup"><span data-stu-id="3fddd-131">The `Run` function checks for the presence of a `validationToken` query parameter.</span></span> <span data-ttu-id="3fddd-132">如果存在此参数，则它会将请求作为 [验证请求](https://docs.microsoft.com/graph/webhooks#notification-endpoint-validation)进行处理，并相应地做出响应。</span><span class="sxs-lookup"><span data-stu-id="3fddd-132">If that parameter is present, it processes the request as a [validation request](https://docs.microsoft.com/graph/webhooks#notification-endpoint-validation), and responds accordingly.</span></span>
- <span data-ttu-id="3fddd-133">如果请求不是验证请求，则 JSON 有效负载被反序列化为 `NotificationList` 。</span><span class="sxs-lookup"><span data-stu-id="3fddd-133">If the request is not a validation request, the JSON payload is deserialized into a `NotificationList`.</span></span>
- <span data-ttu-id="3fddd-134">将检查列表中的每个通知以查找预期的客户端状态值，并对其进行处理。</span><span class="sxs-lookup"><span data-stu-id="3fddd-134">Each notification in the list is checked for the expected client state value, and is processed.</span></span>
- <span data-ttu-id="3fddd-135">将使用 Microsoft Graph 检索触发通知的消息。</span><span class="sxs-lookup"><span data-stu-id="3fddd-135">The message that triggered the notification is retrieved with Microsoft Graph.</span></span>

## <a name="implement-setsubscription-function"></a><span data-ttu-id="3fddd-136">实现 SetSubscription 函数</span><span class="sxs-lookup"><span data-stu-id="3fddd-136">Implement SetSubscription function</span></span>

<span data-ttu-id="3fddd-137">在本节中，您将实现 SetSubscription 函数。</span><span class="sxs-lookup"><span data-stu-id="3fddd-137">In this section, you'll implement the SetSubscription function.</span></span> <span data-ttu-id="3fddd-138">此函数将充当测试应用程序调用的 API，以在用户的收件箱中创建或删除订阅。</span><span class="sxs-lookup"><span data-stu-id="3fddd-138">This function will act as an API that is called by the test application to create or delete a subscription on a user's inbox.</span></span>

1. <span data-ttu-id="3fddd-139">在名为 " **SetSubscriptionPayload.cs** " 的 **模型** 目录中创建一个新文件，并添加以下代码。</span><span class="sxs-lookup"><span data-stu-id="3fddd-139">Create a new file in the **Models** directory named **SetSubscriptionPayload.cs** and add the following code.</span></span>

    :::code language="csharp" source="../demo/GraphTutorial/Models/SetSubscriptionPayload.cs" id="SetSubscriptionPayloadSnippet":::

1. <span data-ttu-id="3fddd-140">打开 **/GraphTutorial/SetSubscription.cs** ，并将其全部内容替换为以下内容。</span><span class="sxs-lookup"><span data-stu-id="3fddd-140">Open **./GraphTutorial/SetSubscription.cs** and replace its entire contents with the following.</span></span>

    :::code language="csharp" source="../demo/GraphTutorial/SetSubscription.cs" id="SetSubscriptionSnippet":::

<span data-ttu-id="3fddd-141">请花点时间考虑 **SetSubscription.cs** 中的代码的功能。</span><span class="sxs-lookup"><span data-stu-id="3fddd-141">Take a moment to consider what the code in **SetSubscription.cs** does.</span></span>

- <span data-ttu-id="3fddd-142">`Run`函数读取在 POST 请求中发送的 JSON 有效负载，以确定请求类型 (订阅或取消订阅) 、订阅的用户 id 以及订阅 ID 取消订阅。</span><span class="sxs-lookup"><span data-stu-id="3fddd-142">The `Run` function reads the JSON payload sent in the POST request to determine the request type (subscribe or unsubscribe), the user ID to subscribe for, and the subscription ID to unsubscribe.</span></span>
- <span data-ttu-id="3fddd-143">如果请求是订阅请求，则使用 Microsoft Graph SDK 在指定用户的收件箱中创建新的订阅。</span><span class="sxs-lookup"><span data-stu-id="3fddd-143">If the request is a subscribe request, it uses the Microsoft Graph SDK to create a new subscription in the specified user's inbox.</span></span> <span data-ttu-id="3fddd-144">在创建或更新邮件时，订阅将会发出通知。</span><span class="sxs-lookup"><span data-stu-id="3fddd-144">The subscription will notify when messages are created or updated.</span></span> <span data-ttu-id="3fddd-145">新订阅在响应的 JSON 有效负载中返回。</span><span class="sxs-lookup"><span data-stu-id="3fddd-145">The new subscription is returned in the JSON payload of the response.</span></span>
- <span data-ttu-id="3fddd-146">如果请求是取消订阅请求，则使用 Microsoft Graph SDK 删除指定的订阅。</span><span class="sxs-lookup"><span data-stu-id="3fddd-146">If the request is an unsubscribe request, it uses the Microsoft Graph SDK to delete the specified subscription.</span></span>

## <a name="call-setsubscription-from-the-test-app"></a><span data-ttu-id="3fddd-147">从测试应用程序调用 SetSubscription</span><span class="sxs-lookup"><span data-stu-id="3fddd-147">Call SetSubscription from the test app</span></span>

<span data-ttu-id="3fddd-148">在本节中，您将实现在测试应用程序中创建和删除订阅的函数。</span><span class="sxs-lookup"><span data-stu-id="3fddd-148">In this section, you'll implement functions to create and delete subscriptions in the test app.</span></span>

1. <span data-ttu-id="3fddd-149">打开 **/TestClient/azurefunctions.js** 并添加以下函数。</span><span class="sxs-lookup"><span data-stu-id="3fddd-149">Open **./TestClient/azurefunctions.js** and add the following function.</span></span>

    :::code language="javascript" source="../demo/TestClient/azurefunctions.js" id="createSubscriptionSnippet":::

    <span data-ttu-id="3fddd-150">此代码调用 `SetSubscription` Azure 函数来订阅，并将新订阅添加到会话中的订阅数组中。</span><span class="sxs-lookup"><span data-stu-id="3fddd-150">This code calls the `SetSubscription` Azure Function to subscribe and adds the new subscription to the array of subscriptions in the session.</span></span>

1. <span data-ttu-id="3fddd-151">将以下函数添加到 **azurefunctions.js** 。</span><span class="sxs-lookup"><span data-stu-id="3fddd-151">Add the following function to **azurefunctions.js**.</span></span>

    :::code language="javascript" source="../demo/TestClient/azurefunctions.js" id="deleteSubscriptionSnippet":::

    <span data-ttu-id="3fddd-152">此代码调用 `SetSubscription` Azure 函数，从会话中的订阅数组中删除订阅。</span><span class="sxs-lookup"><span data-stu-id="3fddd-152">This code calls the `SetSubscription` Azure Function to un and removes the subscription from the array of subscriptions in the session.</span></span>

1. <span data-ttu-id="3fddd-153">如果您没有 ngrok 在运行，请运行 ngrok (`ngrok http 7071`) 并复制 HTTPS 转发 URL。</span><span class="sxs-lookup"><span data-stu-id="3fddd-153">If you do not have ngrok running, run ngrok (`ngrok http 7071`) and copy the HTTPS forwarding URL.</span></span>

1. <span data-ttu-id="3fddd-154">运行以下命令，将 ngrok URL 添加到用户密码存储中。</span><span class="sxs-lookup"><span data-stu-id="3fddd-154">Add the ngrok URL to the user secrets store by running the following command.</span></span>

    ```Shell
    dotnet user-secrets set ngrokUrl "YOUR_NGROK_URL_HERE"
    ```

    > [!IMPORTANT]
    > <span data-ttu-id="3fddd-155">如果重新启动 ngrok，您将需要重复此命令来更新 ngrok URL。</span><span class="sxs-lookup"><span data-stu-id="3fddd-155">If you restart ngrok, you will need to repeat this command to update your ngrok URL.</span></span>

1. <span data-ttu-id="3fddd-156">将您的 CLI 中的当前目录更改为 **/GraphTutorial** 目录，并运行以下命令以在本地启动 Azure 函数。</span><span class="sxs-lookup"><span data-stu-id="3fddd-156">Change the current directory in your CLI to the **./GraphTutorial** directory and run the following command to start the Azure Function locally.</span></span>

    ```Shell
    func start
    ```

1. <span data-ttu-id="3fddd-157">刷新 SPA 并选择 " **订阅** " 导航项。</span><span class="sxs-lookup"><span data-stu-id="3fddd-157">Refresh the SPA and select the **Subscriptions** nav item.</span></span> <span data-ttu-id="3fddd-158">输入包含 Exchange Online 邮箱的 Microsoft 365 组织中的用户的用户 ID。</span><span class="sxs-lookup"><span data-stu-id="3fddd-158">Enter a user ID for a user in your Microsoft 365 organization that has an Exchange Online mailbox.</span></span> <span data-ttu-id="3fddd-159">这可以是 `id` 来自 Microsoft Graph 的用户 () 或用户的 `userPrincipalName` 。</span><span class="sxs-lookup"><span data-stu-id="3fddd-159">This can either be the user's `id` (from Microsoft Graph) or the user's `userPrincipalName`.</span></span> <span data-ttu-id="3fddd-160">单击 " **订阅** "。</span><span class="sxs-lookup"><span data-stu-id="3fddd-160">Click **Subscribe**.</span></span>

1. <span data-ttu-id="3fddd-161">页面刷新显示表中的新订阅。</span><span class="sxs-lookup"><span data-stu-id="3fddd-161">The page refreshes showing the new subscription in the table.</span></span>

1. <span data-ttu-id="3fddd-162">向用户发送电子邮件。</span><span class="sxs-lookup"><span data-stu-id="3fddd-162">Send an email to the user.</span></span> <span data-ttu-id="3fddd-163">经过一小段时间后， `Notify` 应调用该函数。</span><span class="sxs-lookup"><span data-stu-id="3fddd-163">After a brief time, the `Notify` function should be called.</span></span> <span data-ttu-id="3fddd-164">您可以在 ngrok web 界面 (`http://localhost:4040`) 或在 Azure Function 项目的调试输出中进行验证。</span><span class="sxs-lookup"><span data-stu-id="3fddd-164">You can verify this in the ngrok web interface (`http://localhost:4040`) or in the debug output of the Azure Function project.</span></span>

    ```Shell
    ...
    [7/8/2020 7:33:57 PM] The following message was created:
    [7/8/2020 7:33:57 PM] Subject: Hi Megan!, ID: AAMkAGUyN2I4N2RlLTEzMTAtNDBmYy1hODdlLTY2NTQwODE2MGEwZgBGAAAAAAA2J9QH-DvMRK3pBt_8rA6nBwCuPIFjbMEkToHcVnQirM5qAAAAAAEMAACuPIFjbMEkToHcVnQirM5qAACHmpAsAAA=
    [7/8/2020 7:33:57 PM] Executed 'Notify' (Succeeded, Id=9c40af0b-e082-4418-aa3a-aee624f30e7a)
    ...
    ```

1. <span data-ttu-id="3fddd-165">在测试应用程序中，单击订阅的表行中的 " **删除** "。</span><span class="sxs-lookup"><span data-stu-id="3fddd-165">In the test app, click **Delete** in the table row for the subscription.</span></span> <span data-ttu-id="3fddd-166">页面将刷新，且订阅不再位于表中。</span><span class="sxs-lookup"><span data-stu-id="3fddd-166">The page refreshes and the subscription is no longer in the table.</span></span>
